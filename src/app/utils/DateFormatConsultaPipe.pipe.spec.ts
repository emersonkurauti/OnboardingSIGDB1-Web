/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateFormatConsultaPipePipe } from './DateFormatConsultaPipe.pipe';

describe('Pipe: DateFormatConsultaPipee', () => {
  it('create an instance', () => {
    let pipe = new DateFormatConsultaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
